{
  "name": "videoscriber",
  "productName": "Videoscriber",
  "version": "1.0.0",
  "description": "AI-powered video transcription with speaker identification and recap emails",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "fetch-ffmpeg": "bash scripts/fetch-ffmpeg.sh",
    "package": "npm run fetch-ffmpeg && electron-forge package",
    "make": "npm run fetch-ffmpeg && electron-forge make"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.1"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "@electron-forge/cli": "^7.0.0",
    "@electron-forge/maker-dmg": "^7.0.0",
    "@electron-forge/maker-zip": "^7.0.0"
  }
}
